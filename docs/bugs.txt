Примерный текст для вводной.
Википедия это хорошо, но чтобы с ее помощью создать цельную картину знаний нужно проделать поистине титаническую работу по перелопачиванию всех статей.
А если вы еще хотите что в этой картине присутсвовала оценка обоснованности теории и достоверности фактов на которые
она опирается, вам потребуется еще больше усилий. Нужно сформулировать для себя четкий критерий как вы будете определять достоверность факта,
далее для каждого факта эту достоверность оценить, составить байесовский граф аргументов, понять какие теории/модели лучше или хуже в соответствии с тем что получилось. 
И тогда, после 10ти лет работы вы сможете, наконец понять чему стоило посвящать свою жизнь.
Я предлагаю краудсорсить этот процесс.

Создать нового пользователя http://my.grasp.how/createNewUser/<login>/<password>/<admin secret>
Обновить пароль пользователя http://my.grasp.how/updateUserPassword/<login>/<password>/<admin_secret>

Создание нового графа http://grasp.local/createNewGraph?data={"name":"newName"}
Удалить граф http://grasp.local/removeGraph?data={"graph_id":"1234567"}
Скопировать граф вместе с историей http://grasp.local/copyGraph?data={"name":"newName","graph_id":"123456"}
Склонировать граф http://grasp.local/cloneGraph?data={"graph_id":"222","history_step":"333"}
Запуск тестов http://grasp.local/lib/client/jasmine/jasmin.php

- надо описать интерфейс YOVALUE.CanvasDrawer.Circle, YOVALUE.CanvasDrawer.Text, YOVALUE.CanvasDrawer.Path, YOVALUE.CanvasDrawer.Rect
(иначе он начинает расползаться - this.backgroundShape._id в GraphView.js, shape._id в GraphViewElement.js)
- если править название а потом мышкой навести на этот же узел, то остается старое название
- зачем нужна инструкция node.update, если YOVALUE.iGraphModelChanges не предполагает даже nodes.update изменений?
- зачем вообще в GraphModel есть nodeTypes, edgeTypes, nodeDefautType, edgeDefautType если в YOVALUE.iGraphModelEdge, YOVALUE.iGraphModelNode нет поля type?

TODO нужные фичи
- сделать timeline

- Сделать карту википедии по следующему принципу. 
Крупность статьи - это максимум или среднее цитируемость научных работ, которые в ней упоминаются.
Связь между статьей зеленая если она не в разделе критика и синяя если из этого раздела.
Кластеризовать по научным категориям. Связям приписывать фразу из статьи, в которой идет ссылка.

- сделать дефолтные настройки в main.js SelectGraphSkinModel, чтобы не дублировать везде одни и те же настройки скинов в базе
- сделать возможность отображать граф через svg (сейчас только canvas)
- сделать функциональные тесты и вычерчивать диаграммы событий на их основе
- сделать сохранение текста не по каждому символу а с буфером и задержкой
- сделать заметную форму для фидбэка в редакторе
- сделать регистрацию через фейсбук и т.д. на сайте
- уменьшить потребление оперативной памяти на клиенте
- если сервер недоступен (ошибка 503), то запросы накапливаются и, после того, как сервер стал доступен,
все пропадают, потому что уже истек срок авторизации
Если же неизвестная ошибка, то все продолжает делать вид что все хорошо, хотя запрос не сохранился.
Надо: сделать вывод статуса, увеличить время логаута, сделать ввод логина поверх графа

__Распространение__

Надо найти людей которые готовы исследовать чье-то мировоззрение, наносить его на карту
и потом побеседовать с этим человеком дабы окончательно формировать карту